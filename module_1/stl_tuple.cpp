#include<bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    

    // tuple<string,int,string> t = make_tuple("jui",82,"017382");
    // cout<<get<0>(t)<< " "<<get<1>(t)<<" "<<get<2>(t);

    // tuple<string,int,string> t = make_tuple("jui",82,"017382");
    // auto[name,roll,phoneNumber] = t;
    // cout<<name << " "<<roll <<" "<<phoneNumber;

    return 0;
}